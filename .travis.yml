sudo: required
dist: xenial
language: java
jdk:
- oraclejdk8
addons:
  firefox: "56.0"
  chrome: stable
script:
  mvn clean install verify serenity:aggregate
  -Dstory=home
  -DtestDestination=Chrome
install: true
